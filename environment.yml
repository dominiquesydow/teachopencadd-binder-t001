name: teachopencadd-t001
channels:
  - conda-forge
  - defaults
dependencies:
  - python>=3.8
  - pip
  - numpy<1.24
  - rdkit==2021.09.5
  - chembl_webresource_client
  - tqdm
  - pip:
      - nbgitpuller
